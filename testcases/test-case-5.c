#include<stdio.h>
int main()
{
  unsigned int a = 0x0f;
  long int mylong = 123456l;
  long int i, j;

  for(i=0; i < 10; i++)
  {
    for(j=10; j > 0; j--)
	{
		printf("%d",i);
    }
  }


  diff = x - y;
  int rem = x % y;
  printf ("Total = %d \n", total);

  int result = IncreaseBy10(x);
}

int IncreaseBy10(int x)
{
	return x + 10;
}
