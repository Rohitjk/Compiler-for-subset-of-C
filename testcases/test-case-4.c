#include<stdio.h>
int main()
{
  int x, y;
  long long int total, diff;
  int *ptr;
  int a = 86;

  if (a > 90)
  {
	  printf ("Grade is AA");
  }
  else if (a > 80)
  {
	  printf ("Grade is AB");
  }
  else
  {
	  printf ("Grade is BB");
  }

  x = -10, y = 20;
  x=x*3/2;
  total = x + y;
}

